<% if resource.errors.any? %>
  <div class="error-messages">
    <h3><%= pluralize(resource.errors.count, "error") %> prohibited this from being saved:</h3>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<style>
  .error-messages {
    background: #fee;
    border: 1px solid #fcc;
    border-radius: 4px;
    padding: 15px;
    margin-bottom: 20px;
    color: #c33;
  }

  .error-messages h3 {
    margin: 0 0 10px 0;
    font-size: 1rem;
  }

  .error-messages ul {
    margin: 0;
    padding-left: 20px;
  }
</style>
